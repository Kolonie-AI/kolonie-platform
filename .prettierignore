dist
coverage
node_modules
package-lock.json
CHANGELOG.md

# drizzle-kit's snapshots and journal. Generated on every `generate`, never read
# by a human, and reformatting them only guarantees a diff the next time the
# tool writes them. The migrations themselves are .sql and are reviewed by hand.
packages/db/drizzle/meta

# The generated storage barrel (#271). See .gitignore.
packages/db/src/storage/index.ts

# The enumeration probe (#1190). See .gitignore. It exists for the length of one
# test, and formatting it is a race rather than a rule.
packages/db/vitest.*-probe.config.ts

# Vendored third party, and reformatting it defeats the point of vendoring it.
# `schemas/mcp-registry-server-2025-12-11.schema.json` is the official MCP
# registry's own schema, byte-for-byte as it is served, so that
# `scripts/server-json.test.ts` validates against what the registry validates
# against rather than against our copy of it (kolonie-platform#443).
schemas/

# Measurements, written by `scripts/measure-mcp-catalogue.mjs` (#888). Generated
# on every run and read as evidence rather than as source, so reformatting them
# by hand only guarantees a diff the next measurement undoes.
docs/measurements
