{
  "name": "@kolonie-ai/support-triage-runner",
  "version": "0.1.0",
  "private": true,
  "description": "Reads the support tickets citizens file, matches them against work the Colony already has, and files what is new",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@kolonie-ai/core": "*",
    "@kolonie-ai/db": "*"
  }
}
