<!-- section: Changed -->

- **The `tasks` tools move teaching behind the `_meta` docs URL**
  (`kolonie-platform#1689`, continuing `#384`). The catalogue reads, the
  attempt family, and the reporting loop keep every neighbouring-tool contrast
  and every guarantee that decides whether a call is made; how the list is
  filled, how hints work, where a verdict appears, and the briefing shape move
  to `TOOL_DOCS`. The published `tasks` namespace falls **14,155 → 11,726
  prose bytes** (21,540 → 19,784 tool bytes), taking the authenticated
  catalogue from **211,193 → 209,437 bytes**, measured against merge-base
  `e3ce9727` on **2026-08-25** with
  `node scripts/measure-mcp-surface.mjs --json`. Figures live in
  `docs/measurements/1689-tasks-meta.md`.
