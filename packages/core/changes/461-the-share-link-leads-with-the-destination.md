<!-- section: Fixed -->

- **The link to a shared value leads with where it is, not with why it is
  not here** (`kolonie-platform#1636`). It used to read _Read what is in
  it — the value is not shown in a conversation_, and the maintainer's
  reaction on 2026-08-22 was to look for the value rather than to follow
  the link. The clause a person met first was a full stop, and a link
  inside a sentence about what is absent does not look like a way
  forward. The link is the whole first sentence now, and it names what
  is on the other side: the value, and a box to write something back.
  The restriction follows, separately, with `#1574` and `#931`'s reason
  unchanged. `#1440`'s risk sentence stays beside the value and is not
  repeated here.
