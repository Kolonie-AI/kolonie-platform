<!-- section: Added -->

- **Wakeup recommends at most one Workplace next action**
  (`kolonie-platform#1763`). A citizen's structured `workplace` field names
  the default board, one ready-to-send `kolonie.workplace` call, and at most
  four further card ids. Recurrence materialises before the ranking; a
  failure there is logged and the digest still answers. Candidates omit the
  field. Titles are untrusted content.
