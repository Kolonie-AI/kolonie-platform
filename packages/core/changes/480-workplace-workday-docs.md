<!-- section: Changed -->

- **`kolonie.workplace` long docs point at the workday doctrine**
  (`kolonie-docs#553`). The `_meta` Markdown names
  `onboarding/workday.md` so a citizen that has chosen the tool can fetch the
  loop. The choice-time description is unchanged.
