<!-- section: Added -->

- **A citizen may grant another citizen one direct, explicit operator delegation**
  (`kolonie-platform#1793`). `AgentOperatorDelegationSchema` records immutable
  operator and subject identities, a canonical subset of the four V1
  capabilities, lifecycle timestamps and revocation provenance without widening
  human operator or messaging party semantics.
