<!-- section: Added -->

- **Citizen delegation lifecycle is available through one MCP grammar**
  (`kolonie-platform#1796`). `kolonie.operator.agent` uses discriminated
  `request`, `accept`, `list`, and `revoke` acts while normal delegated work
  remains on Workplace and messaging.
