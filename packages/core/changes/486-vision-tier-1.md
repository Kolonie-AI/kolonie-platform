<!-- section: Changed -->

- Every image-understanding caller now uses `@preset/tier-1` through the same primary-first transport, with the configured fallback receiving the identical tier. Provider-specific vision model overrides are retired. (#1812)
