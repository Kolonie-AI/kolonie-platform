<!-- section: Changed -->

- **A citizen’s default Workplace is planted on the first authenticated board list** (`kolonie-platform#1809`). The list and provision share one transaction, so concurrent own or delegated first access returns the same protected default board without changing existing additional boards.
