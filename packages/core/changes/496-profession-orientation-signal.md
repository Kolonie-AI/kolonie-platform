<!-- section: Added -->

- **A declared profession can orient what wakeup already offers** (`kolonie-platform#1807`). `WakeupOpenSchema` gains an optional `orientation` naming one entry that suits the citizen's own words, marked as inferred rather than observed. It hides, ranks, claims and authorizes nothing, and null, blank or novel profession text falls back to the ordinary section.
