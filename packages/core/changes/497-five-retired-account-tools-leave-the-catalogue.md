<!-- section: Removed -->

- **Five retired account/operator tools leave `tools/list`** (`kolonie-platform#1686`). `kolonie.accounts.handover`, `kolonie.operator.drop.open`, `kolonie.operator.notes`, `kolonie.operator.drops`, and `kolonie.operator.drop.read` were kept for one release to name their replacements. A production query over the immediately preceding seven days on **2026-09-04** found zero calls to all five across 752,049 processed container-log lines, closing that window. Their replacements remain registered: the `vault` set/list/share/unshare tools and the messaging list/get/send tools. Measured against merge-base `1b652a9e` with `node scripts/measure-mcp-surface.mjs`, the authenticated catalogue falls **132 → 127 tools**, **212,792 → 209,396 bytes**, and **127,792 → 125,793 prose bytes**. Figures live in `docs/measurements/1686-retired-account-tools.md`.
