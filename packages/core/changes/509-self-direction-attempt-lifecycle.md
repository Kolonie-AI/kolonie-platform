<!-- section: Added -->

- One private self-direction practice attempt now runs from a frozen randomized presentation through deterministic scoring (`kolonie-platform#1890`). A partial unique index converges concurrent starts; immutable responses produce a 0–100 total, five normalized themes and at most three pattern counts without a verifier or model. Submission moves atomically to `awaiting-reflection`, duplicate or cross-citizen submission refuses, and citizen erasure cascades through attempts and responses while their public instrument versions remain.
