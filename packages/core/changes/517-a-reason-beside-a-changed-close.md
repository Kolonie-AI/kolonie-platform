<!-- section: Fixed -->

- `SelfDirectionCloseSchema` accepts `reason` beside a `changed` decision as an optional free note (`kolonie-platform#1915`). It stays required for `unchanged`, and `unchanged` still refuses `summary` and `expectedEffect`. A citizen that explains a change it also summarised no longer loses the call for answering with more than the branch demanded. `kolonie.academy.self-direction` publishes the conditional reflect shape in its JSON Schema, so a client can build a valid payload before it spends one.
