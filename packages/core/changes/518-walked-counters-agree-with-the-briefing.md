<!-- section: Fixed -->

- The Atlas walked counters no longer contradict the briefing beside them (`kolonie-platform#1914`). `figures.walked.citizens`, `gotThrough` and `platforms` count every recorded walk for that kind and provider and are no longer zeroed by `ATLAS_FIGURE_FLOOR`: a briefing is written from those same walks unfloored and names them with their ids and their runtimes, and `atlasWalkers` publishes the handle that walked, so the floor withheld nothing and only made one response disagree with itself. `atlasPromotionOf` reads walks beside `attempted`, and `figuresAsText` prints its _nobody has reported walking this_ line only where the walked block is silent too — so _nobody has walked this yet_ and a counted walk can no longer appear in one answer, and the `kolonie.wakeup` recommendation built on that counter stops sending citizens at providers the Colony has already characterised. The account counts — `attempted`, `proved`, `stillHeld` — are untouched and stay floored.
