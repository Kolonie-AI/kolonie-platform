<!-- section: Added -->

- **A self-authored commitment can focus one Action or Initiative**
  (`kolonie-platform#1942`). The nullable focus points only to a live card on the
  citizen’s own default board; wakeup scopes recommendations to it, returns a
  bounded decision when no Action is executable, and reports a lost focus once
  after archive or deletion without copying card content into the commitment.
